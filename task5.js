//Create a function that takes the age in years and returns the age in days.

//Examples
//calcAge(65) ➞ 23725

//calcAge(0) ➞ 0

//calcAge(20) ➞ 7300

function calcAge(age) {
  return age * 365;
}

const result = calcAge(65);
const result2 = calcAge(0);
const result3 = calcAge(20);
